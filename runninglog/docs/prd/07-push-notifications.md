# PRD: 푸시 알림 (Push Notifications)

> **문서 버전**: v1.0
> **최종 수정일**: 2026-02-13
> **상태**: Draft
> **관련 디자인**: `runninglog-mobile.pen` - Push Notifications (7GwT4), Push Notifications - Empty (y60uf)

---

## 1. 개요

### 1.1 목적

푸시 알림 화면은 사용자가 수신한 모든 알림을 시간순으로 확인하고 관리할 수 있는 화면이다. 마이페이지 헤더의 벨 아이콘을 통해 진입하며, 챌린지 달성, 댓글 알림, 서비스 업데이트, 러닝화 교체 주기 등 다양한 유형의 알림을 "오늘"과 "이전 알림" 섹션으로 구분하여 표시한다.

### 1.2 사용자 가치

| 가치 | 설명 |
|------|------|
| **알림 일괄 관리** | 수신된 모든 알림을 한 곳에서 확인하고, "전체 읽음"으로 일괄 처리할 수 있다 |
| **읽지 않은 알림 강조** | 읽지 않은 알림을 연한 오렌지 배경(#FFF8F0)과 오렌지 dot으로 시각적으로 구분하여 즉시 파악 |
| **시간순 정리** | "오늘"과 "이전 알림" 섹션으로 시간 기반 정리하여 최신 알림에 집중 |
| **빈 상태 안내** | 알림이 없을 때 직관적인 빈 상태 UI로 혼란 없이 안내 |

### 1.3 타겟 사용자

- 30대 남성 러너
- 챌린지, 댓글 등 앱 내 알림을 주기적으로 확인하는 활성 사용자
- 러닝화 교체 주기 등 유지보수 알림을 활용하는 사용자

---

## 2. 사용자 흐름

### 2.1 메인 흐름

```
[마이페이지 헤더 벨 아이콘 탭]
    |
    v
[푸시 알림 목록 화면]
    |
    ├── [알림이 있는 경우]
    │   ├── "오늘" 섹션: 읽지 않은/읽은 오늘 알림
    │   ├── "이전 알림" 섹션: 과거 알림
    │   └── "전체 읽음" 탭 → 모든 알림 읽음 처리
    |
    ├── [알림이 없는 경우]
    │   └── 빈 상태 UI (bell-off 아이콘 + 안내 문구)
    |
    └── 뒤로가기 → 마이페이지
```

### 2.2 알림 아이템 인터랙션

```
[알림 아이템 탭]
    |
    ├── 챌린지 달성 알림 → 트레이닝/업적 화면으로 이동
    ├── 댓글 알림 → 해당 활동 상세 화면으로 이동
    ├── 서비스 업데이트 → 공지사항 상세로 이동
    └── 러닝화 교체 → 러닝화 관리 화면으로 이동
    |
    v
[해당 알림 읽음 처리]
```

---

## 3. 화면별 상세 스펙

### 3.1 알림 목록 화면 (알림 있는 상태)

> **경로**: `app/notifications/index.tsx`
> **라이트/다크 모드 지원**

#### 3.1.1 레이아웃 구조

```
[SafeArea Top]
├── [헤더]                ── 뒤로가기 + "알림" + "전체 읽음"
├── [헤더 구분선]          ── 1px, $border
└── [스크롤 콘텐츠]
    ├── [오늘 섹션]
    │   ├── "오늘" 라벨
    │   ├── 알림 아이템 1 (읽지 않음)
    │   ├── 구분선
    │   ├── 알림 아이템 2 (읽지 않음)
    │   └── 구분선
    └── [이전 알림 섹션]
        ├── "이전 알림" 라벨
        ├── 구분선
        ├── 알림 아이템 3 (읽음)
        └── 알림 아이템 4 (읽음)
```

#### 3.1.2 헤더

| 속성 | 값 |
|------|------|
| 패딩 | 상하 16px, 좌우 20px |
| 정렬 | space_between |
| 배경 | `$background` (#FFFFFF) |

##### 좌측 영역

| 속성 | 값 |
|------|------|
| 뒤로가기 아이콘 | lucide chevron-left, 24x24px, `$text` (#0D0D0D) |
| 타이틀 | "알림", Montserrat, 20px, Bold (700), `$text` |
| 간격 | gap: 12px |

##### 우측 영역

| 속성 | 값 |
|------|------|
| 텍스트 | "전체 읽음" |
| 폰트 | Inter, 14px, Medium (500) |
| 색상 | `$BrandOrange` (#FF6F00) — 읽지 않은 알림이 있을 때 |
| 색상 (비활성) | `$textTertiary` (#9CA3AF) — 모든 알림이 읽음 상태일 때 |
| 탭 동작 | 모든 알림을 읽음 처리 API 호출 |

#### 3.1.3 헤더 구분선

| 속성 | 값 |
|------|------|
| 색상 | `$border` (#E5E5E5) |
| 높이 | 1px |

#### 3.1.4 오늘 섹션

##### 섹션 라벨

| 속성 | 값 |
|------|------|
| 텍스트 | "오늘" |
| 폰트 | Inter, 13px, SemiBold (600), `$textSecondary` (#6B7280) |
| 패딩 | 상하 12px, 좌우 20px |

##### 읽지 않은 알림 아이템

| 속성 | 값 |
|------|------|
| 배경 | #FFF8F0 (연한 오렌지) |
| 패딩 | 상하 16px, 좌우 20px |
| 간격 | gap: 12px |
| 정렬 | 수직 상단 정렬 |

##### 알림 아이템 구성 요소

**아이콘 래퍼**

| 속성 | 값 |
|------|------|
| 크기 | 40x40px |
| 모서리 | cornerRadius: 20 (원형) |
| 정렬 | 중앙 (alignItems: center, justifyContent: center) |
| 이모지 | 18px |

| 알림 유형 | 이모지 | 배경 색상 |
|-----------|--------|-----------|
| 챌린지 달성 | 🏃 | #FFEDD5 (연한 오렌지) |
| 댓글 | 💬 | #E0F2FE (연한 파란) |
| 서비스 업데이트 | 🎁 | #F3E8FF (연한 보라) |
| 러닝화 교체 | 👟 | #FEF3C7 (연한 노란) |

**알림 콘텐츠**

| 요소 | 스타일 |
|------|--------|
| 제목 | Inter, 15px, SemiBold (600), `$text` — 읽지 않은 알림 |
| 제목 | Inter, 15px, Medium (500), `$text` — 읽은 알림 |
| 설명 | Inter, 13px, Regular, `$textSecondary`, 고정 너비 (fill_container) |
| 시간 | Inter, 12px, Regular, `$textTertiary` |
| 간격 | gap: 4px |

**읽지 않음 dot**

| 속성 | 값 |
|------|------|
| 크기 | 8x8px 원형 |
| 색상 | `$BrandOrange` (#FF6F00) |
| 위치 | 알림 아이템 우측 끝 (수직 중앙) |
| 표시 조건 | 해당 알림이 읽지 않은 상태일 때만 |

#### 3.1.5 알림 아이템 간 구분선

| 속성 | 값 |
|------|------|
| 색상 | `$border` (#E5E5E5) |
| 높이 | 1px |

#### 3.1.6 이전 알림 섹션

##### 섹션 라벨

| 속성 | 값 |
|------|------|
| 텍스트 | "이전 알림" |
| 폰트 | Inter, 13px, SemiBold (600), `$textSecondary` |
| 패딩 | 상하 12px, 좌우 20px |

##### 읽은 알림 아이템

| 속성 | 값 |
|------|------|
| 배경 | `$background` (#FFFFFF) — 기본 배경 (읽음 상태이므로 하이라이트 없음) |
| 패딩 | 상하 16px, 좌우 20px |
| 간격 | gap: 12px |
| 제목 fontWeight | Medium (500) — SemiBold보다 얇게 |
| 읽지 않음 dot | 표시하지 않음 |

---

### 3.2 빈 상태 화면 (알림 없음)

> **경로**: 동일 화면, 조건부 렌더링

#### 3.2.1 레이아웃 구조

```
[SafeArea Top]
├── [헤더]                ── 뒤로가기 + "알림" + "전체 읽음" (비활성)
├── [헤더 구분선]          ── 1px, $border
└── [빈 상태 바디]         ── 중앙 정렬된 안내 UI
```

#### 3.2.2 헤더

| 속성 | 값 |
|------|------|
| "전체 읽음" 색상 | `$textTertiary` (#9CA3AF) — 비활성 |
| 기타 속성 | 알림 목록 화면 헤더와 동일 |

#### 3.2.3 빈 상태 바디

| 속성 | 값 |
|------|------|
| 높이 | fill_container |
| 정렬 | 수직/수평 중앙 (alignItems: center, justifyContent: center) |
| 내부 간격 | gap: 16px |
| 레이아웃 | vertical |

##### 빈 상태 아이콘

| 속성 | 값 |
|------|------|
| 아이콘 래퍼 크기 | 80x80px |
| 래퍼 모서리 | cornerRadius: 40 (원형) |
| 래퍼 배경 | `$surface` (#F5F5F5) |
| 아이콘 | lucide bell-off, 36x36px, `$textTertiary` (#9CA3AF) |
| 정렬 | 중앙 |

##### 빈 상태 텍스트

| 요소 | 스타일 |
|------|--------|
| 메인 텍스트 | "알림이 없습니다", Inter, 16px, Medium (500), `$textSecondary` (#6B7280) |
| 서브 텍스트 | "새로운 알림이 오면 여기에 표시됩니다.", Inter, 13px, Regular, `$textTertiary` (#9CA3AF) |

---

## 4. 데이터 모델

### 4.1 알림 아이템 (NotificationItem)

```typescript
interface NotificationItem {
  id: string;                          // 알림 고유 ID
  userId: string;
  type: NotificationType;              // 알림 유형
  title: string;                       // "챌린지 달성!"
  description: string;                 // "'매일 5km' 챌린지를 완료했어요."
  emoji: string;                       // "🏃"
  iconBgColor: string;                 // "#FFEDD5"
  isRead: boolean;                     // 읽음 여부
  createdAt: string;                   // ISO 8601
  timeDisplay: string;                 // "방금 전", "2시간 전", "어제", "2일 전"
  actionUrl?: string;                  // 탭 시 이동할 딥링크 URL
  relatedId?: string;                  // 관련 엔티티 ID (활동, 챌린지 등)
}

type NotificationType =
  | 'challenge_complete'     // 챌린지 달성
  | 'comment'                // 댓글
  | 'service_update'         // 서비스 업데이트
  | 'shoe_replacement'       // 러닝화 교체 주기
  | 'weekly_summary'         // 주간 요약
  | 'achievement'            // 업적 달성
  | 'marketing';             // 마케팅 알림
```

### 4.2 알림 목록 응답 (NotificationListResponse)

```typescript
interface NotificationListResponse {
  notifications: NotificationItem[];
  unreadCount: number;                 // 읽지 않은 알림 총 수
  hasMore: boolean;                    // 추가 페이지 존재 여부
  nextCursor?: string;                 // 커서 기반 페이지네이션
}
```

### 4.3 알림 섹션 구분 (클라이언트 가공)

```typescript
interface NotificationSections {
  today: NotificationItem[];           // 오늘 수신한 알림
  previous: NotificationItem[];        // 이전 알림
}
```

---

## 5. API 요구사항

### 5.1 알림 목록 조회

```
GET /api/v1/notifications/?cursor={cursor}&limit={limit}
Authorization: Bearer {access_token}
```

| 항목 | 스펙 |
|------|------|
| 파라미터 | `cursor` (string, 선택), `limit` (number, 기본: 20) |
| 응답 200 | `NotificationListResponse` 객체 |
| 정렬 | 최신순 (createdAt 내림차순) |
| 비고 | 커서 기반 페이지네이션으로 무한 스크롤 지원 |

### 5.2 알림 읽음 처리 (개별)

```
PATCH /api/v1/notifications/{notificationId}/read/
Authorization: Bearer {access_token}
```

| 항목 | 스펙 |
|------|------|
| 응답 200 | `{ success: true }` |
| 동작 | 해당 알림의 `isRead`를 true로 변경 |

### 5.3 전체 읽음 처리

```
POST /api/v1/notifications/read-all/
Authorization: Bearer {access_token}
```

| 항목 | 스펙 |
|------|------|
| 응답 200 | `{ success: true, updatedCount: number }` |
| 동작 | 사용자의 모든 읽지 않은 알림을 읽음 처리 |

### 5.4 읽지 않은 알림 수 조회

```
GET /api/v1/notifications/unread-count/
Authorization: Bearer {access_token}
```

| 항목 | 스펙 |
|------|------|
| 응답 200 | `{ count: number }` |
| 용도 | 마이페이지 헤더 벨 아이콘의 알림 dot 표시 여부 결정 |
| 폴링 | 30초 간격 또는 푸시 수신 시 갱신 |

---

## 6. 상태 관리

### 6.1 알림 화면 상태

```typescript
interface NotificationScreenState {
  // 알림 데이터
  notifications: NotificationItem[];
  sections: NotificationSections;       // 오늘 / 이전 알림 구분

  // 로딩
  loading: boolean;
  loadingMore: boolean;                 // 추가 페이지 로딩
  error: string | null;

  // 페이지네이션
  hasMore: boolean;
  nextCursor: string | null;

  // 읽음 처리
  markingAllRead: boolean;              // "전체 읽음" 처리 중

  // 읽지 않은 알림 수 (전역)
  unreadCount: number;
}
```

### 6.2 상태 전이 규칙

| 액션 | 상태 변경 |
|------|-----------|
| 화면 진입 | 알림 목록 조회 (`loading = true`), 섹션 자동 분류 |
| 스크롤 끝 도달 | `hasMore`이면 추가 페이지 로드 (`loadingMore = true`) |
| 알림 아이템 탭 | 해당 알림 `isRead = true` (Optimistic UI) → 서버 PATCH → 딥링크 이동 |
| "전체 읽음" 탭 | 모든 알림 `isRead = true` (Optimistic UI) → 서버 POST → `unreadCount = 0` |
| 전체 읽음 실패 | 상태 롤백 + "알림 처리에 실패했습니다" 토스트 |
| 뒤로가기 | 마이페이지로 복귀, `unreadCount` 갱신 |

### 6.3 섹션 분류 로직

```typescript
function classifyNotifications(items: NotificationItem[]): NotificationSections {
  const today = new Date();
  today.setHours(0, 0, 0, 0);

  return {
    today: items.filter(n => new Date(n.createdAt) >= today),
    previous: items.filter(n => new Date(n.createdAt) < today),
  };
}
```

### 6.4 전역 알림 상태 (마이페이지 연동)

```typescript
// 마이페이지에서 사용하는 전역 상태
interface GlobalNotificationState {
  unreadCount: number;                  // 마이페이지 벨 아이콘 dot 표시용
  lastFetchedAt: number | null;         // 마지막 조회 시각
}
```

| 갱신 트리거 | 설명 |
|-------------|------|
| 앱 포그라운드 전환 | `unread-count` API 호출 |
| 푸시 알림 수신 | `unreadCount += 1` |
| 알림 화면 진입 후 복귀 | `unreadCount` 최신값으로 갱신 |
| "전체 읽음" 성공 | `unreadCount = 0` |

---

## 7. 엣지 케이스

### 7.1 알림 목록 관련

| 케이스 | 처리 |
|--------|------|
| **알림 0건** | 빈 상태 화면 (bell-off 아이콘 + "알림이 없습니다" + "새로운 알림이 오면 여기에 표시됩니다") |
| **오늘 알림만 있고 이전 알림 없음** | "오늘" 섹션만 표시, "이전 알림" 섹션 숨김 |
| **이전 알림만 있고 오늘 알림 없음** | "오늘" 섹션 숨김, "이전 알림" 섹션만 표시 |
| **대량 알림 (100건 이상)** | 커서 기반 무한 스크롤, 20건 단위 로딩. 스크롤 끝 도달 시 추가 로드 |
| **동일 알림 중복 수신** | 서버에서 중복 제거 (dedup by `id`), 클라이언트에서도 id 기반 필터링 |

### 7.2 읽음 처리

| 케이스 | 처리 |
|--------|------|
| **"전체 읽음" 중 네트워크 에러** | Optimistic UI 롤백 → 읽지 않음 상태로 복원 + "처리에 실패했습니다" 토스트 |
| **개별 읽음 API 실패** | 로컬에서는 읽음 처리 유지 (사용자 경험 우선), 다음 조회 시 서버 상태로 동기화 |
| **"전체 읽음" 후 새 알림 수신** | 새 알림은 읽지 않음 상태로 목록 상단에 추가, dot 표시 |

### 7.3 네트워크/에러

| 케이스 | 처리 |
|--------|------|
| **오프라인 상태** | 캐시된 알림 목록 표시 + "오프라인 상태입니다" 배너 |
| **알림 목록 로딩 실패** | "알림을 불러올 수 없습니다" + "다시 시도" 버튼 |
| **추가 페이지 로딩 실패** | 하단에 "더 불러오지 못했습니다. 탭하여 재시도" 표시 |

### 7.4 알림 딥링크

| 케이스 | 처리 |
|--------|------|
| **딥링크 대상 화면이 없는 경우** | 알림 탭 무시 (읽음 처리만), 에러 토스트 없음 |
| **삭제된 엔티티 (활동 삭제 후 알림)** | 이동 후 "해당 기록을 찾을 수 없습니다" 안내 |
| **알림 유형 미인식** | 기본 동작 없이 읽음 처리만 수행 |

### 7.5 시간 표시

| 케이스 | 처리 |
|--------|------|
| **방금 (1분 이내)** | "방금 전" |
| **분 단위 (1분~59분)** | "N분 전" |
| **시간 단위 (1시간~23시간)** | "N시간 전" |
| **어제** | "어제" |
| **일 단위 (2일~6일)** | "N일 전" |
| **7일 이상** | "M월 D일" 형식 |
| **작년 이전** | "YYYY.MM.DD" 형식 |

### 7.6 푸시 알림 권한

| 케이스 | 처리 |
|--------|------|
| **푸시 권한 미허용 시 화면 진입** | 알림 목록은 정상 표시 (앱 내 알림). 상단에 "푸시 알림이 꺼져 있습니다. 설정에서 켜주세요." 배너 표시 (선택적) |
| **푸시 권한 OFF 상태에서 "전체 읽음"** | 정상 동작 (앱 내 알림 상태 관리에 푸시 권한 무관) |

---

## 부록: 디자인 토큰 요약

| 토큰 | 값 | 용도 |
|------|------|------|
| `$text` | #0D0D0D | 헤더 타이틀, 알림 제목 |
| `$textSecondary` | #6B7280 | 섹션 라벨, 알림 설명 |
| `$textTertiary` | #9CA3AF | 시간 표시, 빈 상태 서브 텍스트, 비활성 "전체 읽음" |
| `$BrandOrange` | #FF6F00 | 활성 "전체 읽음" 텍스트, 읽지 않음 dot |
| `$background` | #FFFFFF | 화면 배경, 읽은 알림 배경 |
| `$border` | #E5E5E5 | 헤더 구분선, 알림 간 구분선 |
| `$surface` | #F5F5F5 | 빈 상태 아이콘 래퍼 배경 |
| `#FFF8F0` | 직접값 | 읽지 않은 알림 배경 (연한 오렌지) |
| `#FFEDD5` | 직접값 | 챌린지 알림 아이콘 배경 |
| `#E0F2FE` | 직접값 | 댓글 알림 아이콘 배경 |
| `#F3E8FF` | 직접값 | 서비스 업데이트 아이콘 배경 |
| `#FEF3C7` | 직접값 | 러닝화 교체 알림 아이콘 배경 |

### 타이포그래피

| 요소 | 폰트 | 크기 | Weight |
|------|-------|------|--------|
| 헤더 타이틀 "알림" | Montserrat | 20px | Bold (700) |
| "전체 읽음" 버튼 | Inter | 14px | Medium (500) |
| 섹션 라벨 ("오늘", "이전 알림") | Inter | 13px | SemiBold (600) |
| 알림 제목 (읽지 않음) | Inter | 15px | SemiBold (600) |
| 알림 제목 (읽음) | Inter | 15px | Medium (500) |
| 알림 설명 | Inter | 13px | Regular |
| 알림 시간 | Inter | 12px | Regular |
| 빈 상태 메인 텍스트 | Inter | 16px | Medium (500) |
| 빈 상태 서브 텍스트 | Inter | 13px | Regular |
